HTTP_PROXY=http://{{ config.bastion.service_ip }}:{{ config.bastion.squid_http_port }}
HTTPS_PROXY=http://{{ config.bastion.service_ip }}:{{ config.bastion.squid_http_port }}
NO_PROXY={{ config.proxy.no_proxy | join(',') }}
http_proxy=http://{{ config.bastion.service_ip }}:{{ config.bastion.squid_http_port }}
https_proxy=http://{{ config.bastion.service_ip }}:{{ config.bastion.squid_http_port }}
no_proxy={{ config.proxy.no_proxy | join(',') }}
