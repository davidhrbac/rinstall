export HTTP_PROXY=http://{{ config.bastion.service_ip }}:{{ config.bastion.squid_http_port }}
export HTTPS_PROXY=http://{{ config.bastion.service_ip }}:{{ config.bastion.squid_http_port }}
export NO_PROXY={{ config.proxy.no_proxy | join(',') }}
export http_proxy=http://{{ config.bastion.service_ip }}:{{ config.bastion.squid_http_port }}
export https_proxy=http://{{ config.bastion.service_ip }}:{{ config.bastion.squid_http_port }}
export no_proxy={{ config.proxy.no_proxy | join(',') }}
