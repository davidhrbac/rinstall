# direnv example for local operator credentials. Do not commit real .envrc files.
export TF_VAR_vsphere_server="vcenter.example.internal"
export TF_VAR_vsphere_user="administrator@example.internal"
export TF_VAR_vsphere_password="change-me"

# Runtime credentials for the GitLab-managed Terraform HTTP state.
# export TF_HTTP_USERNAME="terraform"
# export TF_HTTP_PASSWORD="change-me"
